<% provide(:title, "Profile") %>

<h3>Profile</h3>
<h4>
  Hello
  <%= image_tag @users.avatar.url if @users.avatar?%>
  <%= image_tag current_user.avatar.url if current_user.avatar.url.present? %>
   <%= image_tag(current_user.avatar_url) if current_user.avatar? %>
    <% if current_user.avatar? %>
      puts "no pic"
      <%else%>
      <%= image_tag @users.avatar.url if @users.avatar?%>
      puts "pic"
    <%end%>
<%= "#{current_user.first_name} #{current_user.last_name}" %>.

</h4>
